import { Routes } from '@angular/router';
import { ProjectsPageComponent } from './tasks/pages/projects-page/projects-page.component';

export const routes: Routes = [
  {
    path: '',
    component: ProjectsPageComponent
  }
];
